info:
  name: Create
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:5283/api/users
  body:
    type: json
    data: |-
      {
        "Name": {
          "Family": "Doe",
          "Given": "John"
        },
        "DateOfBirth": "1990-05-15",
        "Email": "john.doe@fixture.none"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
